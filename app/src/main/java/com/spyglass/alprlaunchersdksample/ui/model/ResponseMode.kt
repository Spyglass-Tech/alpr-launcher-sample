package com.spyglass.alprlaunchersdksample.ui.model

enum class ResponseMode {
    SINGLE,   // The response will be sent only once.
    STREAM    // The response will be sent continuously as a stream.
}